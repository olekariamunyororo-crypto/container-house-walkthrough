# Render Static Site — https://render.com/docs/static-sites
services:
  - type: web
    name: container-house-walkthrough
    runtime: static
    buildCommand: yarn install && yarn build:web
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
